{
  "name": "crypto-dashboard-local",
  "version": "1.0.0",
  "private": true,
  "description": "Local, read-only BTC dashboard for Binance and BingX",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  }
}
